#include <stdio.h>
int main(){
    int arr[100000]={0,}, n, i, x;
    scanf("%d %d", &n, &x);
    for(i=0; i<n; i++){
        scanf("%d", &arr[i]);
    }
    for(i=0; i<n; i++){
        if(arr[i]<x)
            printf("%d ", arr[i]);
    }
}
