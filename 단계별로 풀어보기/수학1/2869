#include <stdio.h>
#include <math.h>
int main(){
    int a,b,v;
    scanf("%d %d %d", &a, &b, &v);
    if(v==a)    printf("%1\n");
    else        printf("%d\n", (int)ceil((v-b-1)/(a-b))+1);
}


//ceil 함수가 무엇인지 배울 수 있었다 (올림 함수)
