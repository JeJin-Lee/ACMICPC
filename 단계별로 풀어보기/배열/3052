#include <stdio.h>
int main(){
    int i, j, cnt=0, arr[10]={0,};
    for(i=0; i<10; i++)
        scanf("%d", &arr[i]);             // 배열에 수를 입력받는다.
        
    for(i=0; i<10; i++){                  // 만약 입력받은 값이 42보다 크거나 같다면, 42로 나눈 나머지로 다시 저장한다.
        if(arr[i]>=42)
            arr[i] %= 42;
    }
    
    for(i=0; i<10; i++){                  // 나머지 값들 중에서 0이 있는지 체크하고, 하나라도 있으면 cnt를 1로 올리고 반복문을 탈출   
        if(arr[i]==0){   // 이 과정을 거치는 이유는, 아래에서 서로 다른 것을 셀 때, 같은 값 중 하나를 0으로 초기화하여 세지 않기 위함이다.
            cnt++;
            break;
        }
    }
    
    for(i=0; i<9; i++){             // 각 값들끼리 값을 비교하고, 같은 값이 있다면, 그 중 하나를 0으로 초기화한다.
        for(j=i+1; j<10; j++){
            if(arr[i]==arr[j])
                arr[j]=0;
        }
    }
    
    for(i=0; i<10; i++){          // 0으로 초기화된 값을 제외하고, 서로 다른 값들이 몇 개인지 세준다. 0인 경우는 12번 행에서 
        if(arr[i]!=0)
            cnt++;
    }
    printf("%d", cnt);
}
